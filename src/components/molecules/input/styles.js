import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
    formControl: {
        width: '100%'
      },
      errorContainer: {
        marginVertical: 5
      },
});

export default Styles;