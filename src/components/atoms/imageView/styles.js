import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  imageView: {
    width: 30,
    height: 30,
  },
  image:{
    width: '100%',
    height: '100%'
  }
});

export default Styles;